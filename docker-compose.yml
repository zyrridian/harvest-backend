version: '3.8'

services:
  farm-marketplace:
    build: .
    container_name: farm-marketplace
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: always

# Note: Database remains in Supabase, so no Postgres service here.
# Just pass your Supabase DATABASE_URL in the .env file.
